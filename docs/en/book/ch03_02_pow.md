# Chapter 3.2: Proof of Work (PoW) Engine

The PoW engine searches for a block hash that satisfies a difficulty target.

Miners vary the nonce until the hash matches the target. Nodes independently recompute the hash and difficulty condition, so validation never depends on trust.

