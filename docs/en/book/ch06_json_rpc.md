# Chapter 6: JSON-RPC 2.0 API

The JSON-RPC API exposes Budlum methods with the `bud_` prefix.

It includes block queries, balance queries, transaction submission, prechecks, metrics, payload limits, and auth-ready configuration fields.

