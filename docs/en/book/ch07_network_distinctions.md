# Chapter 7: Network Distinctions

Mainnet, Testnet, and Devnet use separate chain IDs, ports, configs, genesis state, bootnodes, and local database paths.

This separation prevents replay attacks and accidental data mixing between networks.

