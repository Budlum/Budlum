# Chapter 3.3: Proof of Stake (PoS) Engine and RANDAO

PoS uses validator stake and deterministic selection instead of raw computation.

Budlum uses fixed-point math for deterministic results across platforms, tracks slashing evidence, and updates epochs as the chain advances.

